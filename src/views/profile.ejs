<!doctype html>
<html lang="en">
<head>
   <% include partials/head.ejs%>
  <title>Profile</title>
</head>
<body>
 <% include partials/menu_bar.ejs%>
  <div class="main">  
  <div class="projects">
  <div class="users">
    <h2><%=user.name%></h2>
    <table>
      <tr>
        <th>Email</th>
        <td><%=user.email%></td>
      </tr>
      <%if(profileOwnerName == user.name){%>
        <tr>
          <td> <button  onclick="profile/update">Update</button> </td>
        </tr>
      <%}%>
    </table>
  </div>
  </div>
  </div>
</body>
</html>